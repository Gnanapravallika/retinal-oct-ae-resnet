# TrusthOCTAI Training Hyperparameters Configuration

training:
  epochs: 30
  lr: 0.0001
  weight_decay: 0.0001
  optimizer: "AdamW"
  scheduler: "CosineAnnealingLR"
  patience: 8
  mixed_precision: true
  seed: 42
  device: "cuda"

output:
  dir: "outputs/trustoct"
  save_best_only: true
